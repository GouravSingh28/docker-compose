pipeline {
  agent any

  tools {nodejs "nodejs-14"}

  stages {    
    
            
    stage('Install dependencies') {
      steps {
        sh 'npm --version'
      }
    }     
               
  }
}
