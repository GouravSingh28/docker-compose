pipeline {
  agent any
    tool name: 'nodejs-14', type: 'nodejs'

  stages {    
    
            
    stage('Install dependencies') {
      steps {
        sh 'npm --version'
      }
    }     
               
  }
}
